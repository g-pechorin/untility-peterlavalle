
def rootFolder = file('./')

subprojects {
	apply plugin: 'peterlavalle.untility'
}

project(':input-property.unity') {
	unity {
		link project(':puntility.unity')
	}
}

defaultTasks 'unityExport'
